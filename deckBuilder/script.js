// display creature options in the DOM

//getCreatureInfo: grabs creature info from firebase
const getCreatureInfo = () =>{};

//getCardInfo: grabs card list and info on them for a specific creature
const getCardInfo = (creature) => {};

//addToDeck: adds creatures or card to a deck object
const addToDeck = (creature, card) => {};

//saveDeck: gives the user a unique key to search for their decklist in the future, adds deck object to firebase
const saveDeck = (deckObj) => {};

//handleCardClick: handles both clicks on creatures, their cards, and removing cards/creatures from decks
const handleCardClick = document.addEventListener("", (event) => {})